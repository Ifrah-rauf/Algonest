{
  "lesson_id": 1,
  "lesson_title": "JavaScript Basics + Express Introduction",
  "questions": [
    {
      "id": 1,
      "type": "debug",
      "question": "Why does this Express route keep loading without sending anything back to the client?",
      "code": "app.get('/health', (req, res) => {\n  const status = 'ok';\n});",
      "options": [
        "The route path must use double quotes",
        "No response is sent with res.send() or res.json()",
        "GET routes cannot return plain text",
        "The callback must be async"
      ],
      "correctIndex": 1,
      "difficulty": "easy",
      "topic": "express-routing"
    },
    {
      "id": 2,
      "type": "debug",
      "question": "What is the main bug in this function?",
      "code": "function getUserName(user) {\n  if (user) {\n    let name = user.name;\n  }\n  return name;\n}",
      "options": [
        "name is block-scoped and is not accessible outside the if block",
        "The function must use const instead of let",
        "The return keyword can only be used inside an if block",
        "user.name is not valid JavaScript syntax"
      ],
      "correctIndex": 0,
      "difficulty": "easy",
      "topic": "javascript-variables-scope"
    },
    {
      "id": 3,
      "type": "debug",
      "question": "Why is req.body likely coming as undefined here?",
      "code": "const express = require('express');\nconst app = express();\n\napp.post('/jobs', (req, res) => {\n  res.json({ title: req.body.title });\n});",
      "options": [
        "POST requests do not support JSON bodies",
        "express.json() middleware is missing before the route",
        "req.body only works with GET routes",
        "res.json() must be replaced with res.sendFile()"
      ],
      "correctIndex": 1,
      "difficulty": "medium",
      "topic": "express-middleware"
    },
    {
      "id": 4,
      "type": "mcq",
      "question": "What is Express mainly used for in a Node.js backend project?",
      "options": [
        "Creating HTTP routes, middleware, and APIs on top of Node.js",
        "Replacing JavaScript with SQL",
        "Running React components on the database",
        "Styling frontend pages with CSS"
      ],
      "correctIndex": 0,
      "difficulty": "easy",
      "topic": "express-introduction"
    },
    {
      "id": 5,
      "type": "debug",
      "question": "Why will this async function not return the fetched data correctly?",
      "code": "async function loadJobs() {\n  const response = fetch('/api/jobs');\n  const data = response.json();\n  return data;\n}",
      "options": [
        "fetch() and response.json() are promises and should be awaited",
        "async functions cannot return values",
        "The endpoint must be written without a leading slash",
        "response.json() only works in Express controllers"
      ],
      "correctIndex": 0,
      "difficulty": "medium",
      "topic": "javascript-async-await"
    },
    {
      "id": 6,
      "type": "debug",
      "question": "Why is this route handler failing to read the job id from the URL?",
      "code": "app.get('/jobs/:id', (req, res) => {\n  const jobId = req.query.id;\n  res.json({ jobId });\n});",
      "options": [
        "Route params should be read from req.params.id, not req.query.id",
        "Dynamic route params only work with POST requests",
        "The route should be '/jobs?id'",
        "res.json() removes route parameters"
      ],
      "correctIndex": 0,
      "difficulty": "medium",
      "topic": "express-routing"
    },
    {
      "id": 7,
      "type": "mcq",
      "question": "What does next() do inside Express middleware?",
      "options": [
        "Immediately restarts the Node.js server",
        "Passes control to the next middleware or route handler",
        "Deletes req.body after validation",
        "Automatically sends a JSON response"
      ],
      "correctIndex": 1,
      "difficulty": "easy",
      "topic": "express-middleware"
    },
    {
      "id": 8,
      "type": "debug",
      "question": "What is the issue in this middleware if the user is authorized?",
      "code": "function authMiddleware(req, res, next) {\n  if (!req.headers.authorization) {\n    return res.status(401).json({ error: 'Unauthorized' });\n  }\n}",
      "options": [
        "res.status(401) is invalid in Express",
        "next() is not called when authorization exists, so the request stops",
        "headers cannot be accessed from req.headers",
        "Middleware functions cannot use return"
      ],
      "correctIndex": 1,
      "difficulty": "medium",
      "topic": "express-middleware"
    },
    {
      "id": 9,
      "type": "debug",
      "question": "Why does this server not start correctly?",
      "code": "const express = require('express');\nconst app = express;\n\napp.listen(5000, () => {\n  console.log('Server running');\n});",
      "options": [
        "express must be called as express() to create an app instance",
        "listen() only works on port 3000",
        "console.log() cannot be used inside listen()",
        "The require statement must be inside app.listen()"
      ],
      "correctIndex": 0,
      "difficulty": "easy",
      "topic": "express-app-setup"
    },
    {
      "id": 10,
      "type": "debug",
      "question": "Why is this condition not working as expected when status is 'open'?",
      "code": "const status = 'open';\n\nif (status = 'closed') {\n  console.log('Job is closed');\n}",
      "options": [
        "Single = assigns a value; === should be used for comparison",
        "Strings cannot be checked inside if conditions",
        "console.log() changes the status value",
        "The condition must use ==! instead of ="
      ],
      "correctIndex": 0,
      "difficulty": "easy",
      "topic": "javascript-conditionals"
    },
    {
      "id": 11,
      "type": "debug",
      "question": "Why might this route return [object Object] or a weak response format instead of clean JSON?",
      "code": "app.get('/profile', (req, res) => {\n  const user = { id: 1, name: 'Alex' };\n  res.send('User: ' + user);\n});",
      "options": [
        "Objects should be returned with res.json(user) for a proper JSON response",
        "Objects cannot be created inside route handlers",
        "res.send() only supports numbers",
        "The user object must be declared with let"
      ],
      "correctIndex": 0,
      "difficulty": "medium",
      "topic": "express-response"
    },
    {
      "id": 12,
      "type": "mcq",
      "question": "In JavaScript, what is the main difference between synchronous and asynchronous code?",
      "options": [
        "Synchronous code waits step by step, while asynchronous code can continue and resolve later",
        "Asynchronous code can only be written in Express, not normal JavaScript",
        "Synchronous code always requires await",
        "There is no practical difference"
      ],
      "correctIndex": 0,
      "difficulty": "easy",
      "topic": "javascript-async-basics"
    }
  ]
}
